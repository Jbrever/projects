const search=()=>{
    const searchbox =document.getElementById("search-notes").ariaValueMax.toUpperCase()
    const notesitem=document.getElementById('notes-list')
    const notes=document.querySelectorAll('')
}